import { ResourceLayout } from "@/components/resource/resource-layout"

export default function ResourcesPage({ children }: { children: React.ReactNode }) {
  return <ResourceLayout>{children}</ResourceLayout>
}
