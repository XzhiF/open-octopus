# 07 — Daily Memory Editing

Type: grilling | Status: resolved

## Question
DailyBrowser 日记记忆是否需要支持编辑？

## Answer
**B: 支持编辑。** 用户希望灵活一点，可以在 UI 上修改/追加日记内容。后端 `POST /memory` 已支持 `layer=daily` 的 `appendDaily()` 方法。
