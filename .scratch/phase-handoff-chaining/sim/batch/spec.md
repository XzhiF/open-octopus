# Sim Fixture Spec — matt-spec-dev simulate 场景 4/5（spec-resolve 真执行底本）

> E2E_SIM 固定夹具，非交付 spec。仅为 `matt-spec-dev.test.yaml` 中
> `real_execution: [spec-resolve]` 场景提供批次目录现态（spec.md + issues/ 非空）。

## Solution

无 —— 本文件不被任何实现读取，spec-resolve 只核对存在性与 issues/ 非空。
